# 0.1.0 release preflight — 2026-09-08

## Current preparation update

Production WGSL is now implemented at `73d8b51`; default/editor each pass 205
tests and the production Metal comparisons pass. The 0.1.0 signed candidate
executable is `91c8afdcdc6186ca2efa24ca1c06cbcbb619a659b62e77fbe69fe988f9323a22`.
Its AE cache generations are 7 (default) / 8 (editor). Installation succeeded
after administrator authentication. Scripted AE checks pass (217 assertions),
but real Language Undo fails; this candidate is rejected for publication.
A supervised-callback and idle-publication repair is being built. Publication
remains NOT_RUN. The older preparation snapshot below is retained as history.

A candidate ZIP with installation instructions, examples, authoring skills,
source identity, the exact Cargo.lock, checksums and third-party notices passes
all 15 archive checks. Extraction with `ditto` preserves its executable mode;
the bundled verifier, invoked without a bundle override, finds the root-level
plugin and verifies its signature and unchanged binary hash. Both local image
links in the packaged documentation resolve to copied image files. The
candidate archive SHA-256 is
`10822b29ba432f5b01bd11eb8e6ccf3454615bd52f2c5cc0519c330e54203c54`;
it is a preparation artifact, not an uploaded release asset.

[Dependency notices](../../scripts/release/dependency_notices.py) conservatively
inventory 132 locked normal/build dependencies and the Rust standard library.
All 253 license-file hashes pass after packaging; notices remain outside the
signed bundle and bypass Markdown rewriting. Missing upstream texts use
explicitly sourced supplements with no invented copyright holders. The
[supplement manifest](../../scripts/release/license-supplements/manifest.json)
allows offline reproduction. The generated notice manifest hash is
`2d5db00600b683b5a9b5a77eb6efd50e1d4fd377163c15914f2444b6298d382d`.

The refreshed source scan covers 1,107 files / 47,332,239 bytes and reports
zero recognized credential or supplied restricted-string findings. This
checks the concrete strings recovered during the semantic review below;
it does not recover a missing historical private term list. Governance passes
99 Markdown files / 1,098 local links / 44 Accepted ADRs. These are dated
preparation results; repeat against the final host-accepted publication tree.

## Earlier preparation snapshot

The initial full staged whitespace check also reported whitespace emitted by
raw compiler/aerender logs and a retained patch, plus compressed pixel bytes
that Git had misclassified as text. Raw evidence is preserved unchanged;
`.zlib` is now explicitly binary. Three tool-manifest final blank lines were
removed. Whitespace validation of maintained source/docs excludes retained
evidence and verbatim third-party license inputs; it does not claim that the
raw logs themselves are whitespace-clean.

**Outcome: the existing publication candidate has no recognized credential
findings or observed chat screenshots; the 0.1.0 release artifact is still
pending production WGSL completion and its own host acceptance.** The prior
`e239ae45…` artifact is a verified 0.0.6 GLSL-only development build. It cannot
be relabeled as the 0.1.0 WGSL release.

This is a preflight snapshot at working-tree baseline `d4477ab`, before the
production frontend and final release are frozen. It is not a release PASS.
The current release decision is [ADR-0044](../adr/0044-wgsl-and-010-release.md)
and publication follows [ADR-0036](../adr/0036-single-repository-record.md).

## Short action list

1. Finish production WGSL and set the main Cargo package to 0.1.0. Update
   current language/support/version documentation; retain historical records.
2. Build the final default artifact and test both default/editor source
   flavors. Run ADR-0044's real AE WGSL and GLSL regression obligations on
   the exact default artifact; record the installed executable hash.
3. Freeze it. Archive the already signed bundle with `INSTALL.txt`, MIT
   `LICENSE` and `SHA256SUMS` outside the bundle. Verify version, byte hashes,
   executable mode and extracted signature; do not rebuild or re-sign.
4. Run the publication scan on the final candidate, record the exact commit
   and lockfile, publish regular `v0.1.0` with the scoped host/signing claims,
   then re-download and verify the uploaded bytes. Windows assets follow
   from the same tag with independent build and host evidence.

## Live release convention and platform decision

Read-only `gh release list` and `gh release view v0.0.6` confirmed six public
releases, `v0.0.1` through `v0.0.6`, all marked pre-release. The latest
[v0.0.6 release](https://github.com/JUNKDOGE-JOE/dynamicfx/releases/tag/v0.0.6)
was published at 2026-08-26T11:21:11Z. It has one Windows asset,
`DynamicFX-0.0.6-win-x64.zip`, 3,173,261 bytes, SHA-256
`c5a401f8f2ad272f2dd0178f1258c850357f5b65e861021a412435d9b57fb23d`.
Its notes identify the exact AEX hash and tested host builds. There is no
repository CI workflow that builds a missing Windows artifact automatically.

The first API attempts failed under restricted network access; the same
read-only commands succeeded with network permission. No remote state was
changed. ADR-0044 explicitly authorizes a regular 0.1.0 release with a
macOS ARM asset first and Windows assets pending. That differs from the
previous pre-release convention and must be visible in the notes.

`security find-identity -v -p codesigning` returned zero valid signing
identities. Only the count was retained; no identity or key material was
printed. The current packager applies an ad-hoc hardened-runtime signature.
ADR-0044 permits this distribution and requires a clear **not notarized**
statement and a quarantine-removal step scoped to this plugin. Do not claim
Developer ID signing or disable system-wide Gatekeeper. Scope of initial
support: native Apple Silicon AE 2026, exact tested build. Other macOS years,
Intel/Rosetta and Windows acceptance of this diff remain unverified.

## Publication content review

The initial scan used the tracked plus nonignored working tree:
811 files / 19,333,309 bytes. Recognized credentials: **0 findings**.
Transport `.bridge.json` envelopes and conversation-like `.jsonl` streams:
**0 candidates**. Machine-local profile/cache paths appeared in 64 files,
all original evidence or reproduction harnesses; none was in ordinary
product documentation. Retention is deliberate: command outputs,
installation/backup identities, input/output artifact paths and compiler
diagnostic source locations support the evidence chain. This audit does not
copy those values into another report.

All 34 newly added PNG/PSD files were opened in an image contact sheet and
visually inspected. They contain analytic ramps, shader glow frames, AA/noise
comparisons and WGSL fixture renders. No user chat window, credential UI,
personal photograph or desktop screenshot was observed. The contact sheet
is temporary and not a publication artifact. The research SVG is an original
diagram; its accompanying JSON cites official public media and explicitly
states that the media was not downloaded or republished. No new AEP, video,
plugin executable or private transport envelope is in the publication
candidate. Local AEPs and binaries remain in ignored output directories.

The scanner uses credential signatures and literal-auth patterns, reporting
only file names and categories, never matched values. It does not prove the
absence of every possible secret. The historical private competitor terms
were not supplied to this session, so that exact term scan is **NOT_RUN**;
the script accepts a private terms file without printing its values. The
reviewed new material consists of this project's source, generated shader
output, synthetic test evidence and observations of public media, rather
than extracted third-party binary internals. Do not describe a heuristic
scan as an exhaustive vendor-term scan.

The candidate changes while WGSL implementation continues. Repeat the scan
immediately before the publishing commit/push; the numbers above are a
dated snapshot, not a certification of later files.

Follow-up semantic review found an unnecessary embedded third-party effect
identity and extraction-origin commentary in the previously tracked
`scripts/f003/f003e_refcompare.jsx`. With explicit authorization, this source
was cleaned: the optional comparison now requires an operator-supplied
`DYNAMICFX_REFERENCE_EFFECT` value, has no default, and otherwise dumps only
DynamicFx. The known concrete strings were scanned in memory across the
tracked plus nonignored tree; output recorded file/line locations only.
Before cleanup there were three matches in that script and two in Accepted
ADR-0033 (lines 14 and 83). The script's three matches were removed; the
governance owner then added visible ADR-0033 redactions and an ADR-0036 §4
entry. A repeat known-string/credential scan on the resulting 823-file
candidate found zero matches; the main package version now reads 0.1.0.
The historical private vendor-term list still was not
available, so this focused known-string scan does not replace that wider
check. Published Git history cannot be recalled by changing the new tree.
No Accepted ADR or historical evidence bytes were silently modified; this
paragraph visibly records the source cleanup and corrects any interpretation
of the earlier heuristic scan as a full semantic publication clearance.

The final preparation check reported governance PASS (91 Markdown files,
1,078 local links, 44 Accepted ADRs, zero errors) and `git diff --check`
passed. These values precede the final production/host evidence additions
and must be refreshed for the published candidate.

## Version and package boundaries

| Surface | Required handling |
|---|---|
| Main `Cargo.toml` | Set the package version to 0.1.0 before the final build |
| `Cargo.lock` | Refresh the main package entry through Cargo; retain the exact complete lockfile as release evidence because it is currently ignored |
| macOS Info.plist | Both bundle version fields come from Cargo at packaging time; verify both equal 0.1.0 |
| `build.rs` AE effect version | This is an independent plugin-cache generation, currently 1.5 default / 1.6 editor, not Cargo semver. Review a cache-generation bump for the new Language popup; do not mechanically replace it with 0.1.0 |
| README / language and macOS docs | Update current version, production WGSL instructions, host table and installation path/signing statement |
| Historical ADRs/audits/evidence | Preserve old 0.0.6 identities and results; no global version substitution |
| Quality and WGSL spike Cargo manifests | Their own package versions are independent; do not use them as the shipping plugin version |
| Asset/tag/notes | `v0.1.0`, a versioned macOS ARM ZIP, archive + executable hashes, exact source/host evidence, Windows-pending and not-notarized statements |

The old `DynamicFX-0.0.6-macos-arm64-local.zip` was checked as a negative
release candidate. It has the complete arm64 bundle and executable mode, but
correctly fails the 0.1.0 version check and lacks release-level installation
instructions, license and checksum manifest. These are expected omissions
of that local-development archive; the old signed bytes remain preserved.

Add package documentation beside the signed `.plugin`, not inside it. Adding
or editing a sealed resource after acceptance would invalidate the signature
or require re-signing, breaking the artifact freeze. The final archive must
omit Finder/AppleDouble metadata and preserve executable permission. The
existing [macOS verifier](../../scripts/macos.py) checks Mach-O architecture,
entry point, PiPL, system dependencies and the signature after extraction.
Packaging context describes when packaging ran; it is not proof that an old
binary was built from the current checkout.

## Checks and test boundary

The new [release preflight](../../scripts/release/preflight.py) is read-only:
no network, install, signing, git mutation or release action. Run it with:

```sh
python3 scripts/release/preflight.py --expected-version 0.1.0 --archive dist/DynamicFX-0.1.0-macos-arm64.zip
```

Optional `--restricted-terms-file` reads newline-separated private terms from
a local file; never commit that file. Output includes no matched text. The
script checks the candidate source version, credential patterns, ZIP member
safety, arm64 header, executable mode, required bundle files, package docs
and checksums. It explicitly does not claim native signature/notarization,
host behavior, source equivalence or release authorization.

Seven temporary byte-level fixtures passed: valid archive structure,
rejection of missing instructions, altered checksums, traversal paths,
Finder detritus, wrong version and a synthetic credential. Fixture binaries
and signatures were deliberately fake; these tests exercised archive rules
only. Python syntax/import succeeded without writing bytecode to the repo.

The release host gate is [ADR-0044's verification obligations](../adr/0044-wgsl-and-010-release.md#verification-obligations):
production direct WGSL parsing/reflection and ABI rejection, parameter layout,
source mapping, graph/persistence contracts; equivalent GLSL/WGSL GPU results
at 8/16/32-bpc; real AE menu/defaults/publication, keyframes, invalid source,
language changes, Undo/Redo, save/reopen, Layer/Gradient/Path resources,
temporal random-time requests, Full/Half/Quarter and independent aerender.
The old 184-test GLSL suite and feasibility spike are useful baselines, not
proof of the new frontend. Windows is explicitly deferred by the user; no
Windows PASS or Windows asset should be manufactured from Metal results.

## Remote publication sequence

Prepare the release text in a file and use `gh release create` with
`--verify-tag`, the explicit `v0.1.0` tag and `--notes-file`; do not silently
create a tag from whichever branch happens to be checked out. The source tag
must identify the frozen candidate and the release must not be marked
pre-release under ADR-0044. Publish the full project record permitted by
ADR-0036, including cited evidence; do not publish only the runtime while
leaving PASS artifacts unreachable.

After upload, read back the release flags, tag target and assets. Download the
asset into a fresh temporary directory, compare its archive hash, extract
preserving executable mode, verify its signature and compare its binary/PiPL
hashes with the host-tested identity. A successful upload is not this final
verification. Future Windows backfill must use the same release source/tag,
retain its own host evidence and publish/re-download its own checksum.

**Next exact action:** finish the production frontend and its final artifact
acceptance, then repeat this preflight against the actual 0.1.0 package.
