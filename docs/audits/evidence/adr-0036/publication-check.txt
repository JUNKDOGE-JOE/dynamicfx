ADR-0036 publication verification - consolidated public tree, 2026-08-17
(Scan patterns are held in shell variables and are not reproduced in this
 report: writing the withheld vendor terms here would itself publish them.
 This report file is excluded from its own scans.)

$ python scripts/check_governance.py
DynamicFX governance check
markdown_files=64
local_links=613
mermaid_blocks=16
accepted_adrs=36
errors=0
RESULT=PASS

$ grep -rlaiE "$VENDOR_TERMS" . --exclude-dir=.git --exclude-dir=adr-0036
(no matches)

$ grep -rlaiE "$CREDENTIAL_TERMS" . --exclude-dir=.git --exclude-dir=adr-0036
(no matches)

$ find . -name '*COMPET*' -not -path './.git/*'    # withheld document must be absent
(no matches)

$ grep -rlaE "$LOCAL_PATH_TERMS" . --exclude-dir=.git --exclude-dir=adr-0036
./docs/audits/evidence/f003-20260816/f003b.log
./docs/audits/evidence/layer-origin/pos_repro.jsx
./docs/audits/evidence/m0-transport-spike/ae2025/aerender_s5.txt
./docs/audits/evidence/m0-transport-spike/ae2025/s5.log
./docs/audits/evidence/m1-first-frame/ae2025/aerender_m1.txt
./docs/audits/evidence/m1-first-frame/ae2025/checks.txt
./docs/audits/evidence/m2-keyframed/ae2025/checks.txt
./docs/audits/evidence/m2-keyframed/ae2025-annotation/checks.txt
./docs/audits/evidence/m2-keyframed/ae2025-kinds/checks.txt
./docs/audits/evidence/m3-persistence/ae2025/aerender_m3.txt
./docs/audits/evidence/m3-persistence/ae2025/checks.txt
./docs/audits/evidence/m4-multipass/ae2025/checks.txt
./docs/audits/evidence/m5-pixel-formats/ae2025/checks.txt
./docs/audits/evidence/m6-temporal/ae2025/m6all.log
./docs/audits/evidence/m7-perf/ae2025/m7bench.log
./docs/audits/evidence/m7-perf/ae2025/m7bench_run1_20s.log
./docs/audits/evidence/m7-perf/ae2025/wysiwyg/wys2_setup.jsx
./scripts/f003/run_f003.ps1
./scripts/m1/run_m1.ps1
./scripts/m2/run_m2.ps1
./scripts/m3/run_m3.ps1
./scripts/m4/run_m4.ps1
./scripts/m5/run_m5.ps1
./scripts/m6/run_m6.ps1
./scripts/m7/run_m7.ps1
./scripts/spike/run_spike.ps1
  -> 26 files, retained deliberately. They are original evidence artifacts and
     the reproduction harnesses that produced them; the paths are the project
     directory and an anonymous Windows account name, which is what an evidence
     record needs to be reproducible (ADR-0036 section 5). No credential, no
     personal identity, no third-party data. Editing them would alter evidence.

----------------------------------------------------------------------------
Push scan, 2026-08-19 (before pushing 85bd676..26a906e + this record)
Scope: `git diff b1cb0f7..HEAD` (added lines) and the tracked tree at HEAD.
Patterns held in shell variables as above; not reproduced here.

$ python scripts/check_governance.py
RESULT=PASS  (errors=0, accepted_adrs=37)

$ git diff b1cb0f7..HEAD | grep -i -c "$VENDOR_TERMS"        -> 0
$ git grep -i -l "$VENDOR_TERMS" HEAD                        -> (no matches)
$ withheld document present in HEAD tree?                    -> absent
$ added lines matching "$CREDENTIAL_TERMS"                   -> 0
$ added lines matching "$LOCAL_PATH_TERMS"                   -> 0
$ host identifiers added: GPU model/driver and Windows build in the two new
  evidence READMEs and TR rows -- the same identifiers already published in
  TR-0037-001 / TR-REL-004; required by the evidence policy, retained.
Content pushed: TR-BIND-002 defect record + evidence (issue #6),
examples/apple-thermal.glsl + README + compile test + TR-EX-001 evidence
(own renders only; no third-party footage).

----------------------------------------------------------------------------
Push scan, 2026-08-21 (before pushing 3d1950c..eefb365 + this record)
Scope: `git diff 3d1950c..HEAD` (added lines) and the tracked tree at HEAD.
Patterns held in shell variables as before; not reproduced here.

$ python scripts/check_governance.py
RESULT=PASS  (errors=0, accepted_adrs=37)

$ git diff 3d1950c..HEAD | grep '^+' | grep -i "$VENDOR_TERMS"        -> 0
$ added lines with binary-analysis markers (PIPL/OutFlags/decoded/extracted) -> 0
$ withheld competitor document present in HEAD tree?                  -> absent
$ git ls-files | grep -iE "compet|vendor"                            -> (no matches)
$ added lines matching "$CREDENTIAL_TERMS"                            -> 0
  (the only token= in the added content is the benign 51-bit session
   token 3763537382884453, source-derived, already published in TR-BIND-002)
$ ae-mcp panel client aemcp.py (holds the auth-token path) published?  -> no,
  not copied into the evidence dir; the copied harness scripts only `import
  aemcp` and carry no token or token path.
$ added lines matching "$LOCAL_PATH_TERMS"                            -> 3 files
  ./docs/TEST_MATRIX.md                     (AE install paths, as existing TR rows)
  ./docs/audits/evidence/field-20260821-prism-sample/README.md
  ./docs/audits/evidence/field-20260821-prism-sample/copy_flicker_repro.py
  -> retained deliberately: the project dir, an anonymous Windows account name
     ("A"), the scratchpad session dir, and the AE 2026 install path — exactly
     what the evidence policy needs to be reproducible (ADR-0036 section 5).
     No credential, no personal identity, no third-party data.

Content pushed: the two 0.0.4 field-defect records on the prism sample
(TR-CACHE-001 / issue #7 interrupted-render cache poisoning, and the
TR-BIND-002 / issue #6 copy-flicker AE-2026 confirmation) with evidence;
the SmartRender InterruptCancel fix (cfccd5d) and its AE-2026 host
verification. Host identifiers added (GPU model/driver, Windows build, AE
2026 build, artifact hashes) are the same class already published in
TR-REL-004 / TR-Y26-001, required by the evidence policy.

Note on own-content: cached-frame-missing-layer1.png and the post-fix
renders are frames of the user's `prism` composition (an abstract
chromatic-dispersion flare, no text/identifying content), published at the
user's direction as defect evidence — comparable to the example renders
already in the record. The user's `.aep` file itself is not committed.

----------------------------------------------------------------------------
Push scan, 2026-08-21 (before pushing e588830..357b482 + this record; release 0.0.5)
Scope: `git diff e588830..HEAD` (3141 added lines in 29 files) and the tracked
tree at HEAD. Scanner: a small Python script applying the same classes of
pattern as before (credential terms, personal e-mail, user-profile/scratchpad
paths, project-dir paths, binary-analysis markers, competitor/vendor words);
patterns not reproduced here.

$ python scripts/check_governance.py
RESULT=PASS  (errors=0, accepted_adrs=38)

credential terms in added lines                                       -> 2, both benign:
  scripts/aemcp.py reads the ae-mcp panel token from the user's own
  `~/.ae-mcp/auth-token` at run time (a generic profile-relative path; no
  value is stored or printed — the health output strips it), and the
  evidence README says so in words. The client is published this time
  because the evidence scripts import it; it carries no auth material.
personal e-mail addresses in added lines                              -> 0
user-profile / scratchpad paths in added lines                        -> 0
  (harness reports write `%TEMP%`; the copied reports were scrubbed)
project-dir paths (E:\Code\AePlugin_Dynamicfx) in added lines          -> 52 lines, 6 files:
  the curated M2/M3 runner logs (PIXEL probe lines naming the output PNGs)
  and four `aerender_stdout.txt` files naming their output folder
  -> retained deliberately, as in the previous scans: the evidence needs the
     artefact paths to be reproducible (ADR-0036 section 5); no credential,
     no personal identity, no third-party data.
binary-analysis markers (decoded/extracted/...) in added lines        -> 2, both our own
  `persistence.rs` test decoding our own snapshot format; nothing about a
  third-party binary.
competitor/vendor words in added lines                                -> 0
withheld competitor document present in HEAD tree?                    -> absent
$ git ls-files | grep -iE "compet|vendor"                             -> (no matches)
token files in the tracked tree (auth-token / *.token / .env)         -> (none)

Content pushed: ADR-0038 (Accepted) and its implementation — the registry
keyed per (source, plan) with the hidden PlanToken stream — the TR-BIND-002
harness with pre-fix FAIL and fix-build PASS evidence on AE 2025 + 2026,
the fix-build M2/M3 battery logs (TR-0038-001), the 0.0.5 release record
(TR-REL-005) and the package version bump. Host identifiers (GPU model,
Windows build, AE builds, artifact hashes) are the same class already
published in TR-REL-004 / TR-Y26-001 and required by the evidence policy.
The harness PNG/PSD frames are synthetic probe renders (coloured quadrants
over solid layers), not user content.
